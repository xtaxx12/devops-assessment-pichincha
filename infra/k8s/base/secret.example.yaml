# Plantilla de referencia. El Secret real NO se versiona: lo crea el pipeline desde GitHub Secrets
# (o manualmente con `kubectl create secret generic devops-service-secrets ...`).
apiVersion: v1
kind: Secret
metadata:
  name: devops-service-secrets
type: Opaque
stringData:
  API_KEY: replace-me
  JWT_SECRET: replace-me
