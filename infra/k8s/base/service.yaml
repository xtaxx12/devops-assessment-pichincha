apiVersion: v1
kind: Service
metadata:
  name: devops-service
  labels:
    app.kubernetes.io/name: devops-service
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: devops-service
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
